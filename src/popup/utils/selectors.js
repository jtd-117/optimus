/**
 * @file selectors.js
 * @description An enumeration of selector strings for use within `popup` directory
 */
const popupSlrs = Object.freeze({
    SG: "settings",
    CE: "close",
    RY: "replay",
    PY: "play",
    PE: "pause",
    BL: "section.popup.block button.block",
});

export default popupSlrs;
